import React from 'react'

import ContentHolder from '../ContentHolder'

const LoadingSpinner = (): React.ReactElement => {
    return <ContentHolder>Under Construction</ContentHolder>
}

export default LoadingSpinner
